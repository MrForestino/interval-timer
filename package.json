{
  "name": "interval-timer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/interval-timer/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  }
}
